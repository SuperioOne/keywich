# keywitch

```
root/
├── apps/
│   ├── keywitch-cli/
│   │   └── (CLI app)
│   └── keywitch-desktop/
│       └── (Tauri desktop app)
├── packages/
│   ├── keywitch-lib/
│   │   └── (shared library)
│   └── keywitch-gui/
│       └── (Sveltekit GUI)
└── dist/
    ├── aur/
    │   └── (AUR PKGFILE)
    ├── flatpak/
    │   └── (flatpak configurations)
    └── container/
        └── (container configs docker etc.)

```